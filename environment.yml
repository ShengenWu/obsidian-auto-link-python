name: obsidian-auto-link
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - typer[all]>=0.9.0
    - langchain>=0.1.0
    - langchain-community
    - langchain-openai
    - langchain-huggingface
    - chromadb>=0.4.0
    - pydantic>=2.0.0
    - pyyaml>=6.0
    - python-frontmatter>=1.0.0
    - rich>=13.0.0
    - pytest>=7.0.0
